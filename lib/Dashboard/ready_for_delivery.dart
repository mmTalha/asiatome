import 'package:flutter/material.dart';


class ready_for_delivery extends StatelessWidget {
  const ready_for_delivery({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
